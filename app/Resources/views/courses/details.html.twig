{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>{{ results.formations }}</h1>
                {% for module in results.modules %}
                    <p>{{ module.language }}</p>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}